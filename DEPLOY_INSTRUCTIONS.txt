TRUSTCART DEPLOYMENT

1. Upload/push this updated project to your Render-connected Git repository.
2. Render will rebuild using the Dockerfile.
3. Keep these environment variables in Render:
   - SPRING_PROFILES_ACTIVE=postgres
   - SPRING_DATASOURCE_URL=<your Supabase/Postgres JDBC URL>
   - SPRING_DATASOURCE_USERNAME=<your DB username>
   - SPRING_DATASOURCE_PASSWORD=<your DB password>
   - SERVER_ADDRESS=0.0.0.0
   - PORT=8080

URGENT 503 FIX
The outage was caused by an old database category value: PREPARED_FOODS. This package now supports the old value and automatically repairs it to PREPARED_FOOD on startup.

If the site is still down before redeploy finishes, run database/urgent-render-hotfix.sql in Supabase SQL Editor, then redeploy.

WHAT WAS UPDATED
- Fixed PREPARED_FOODS enum crash.
- Reduced chat-thread FK deployment warnings by disabling generated chat FK constraints and adding orphan cleanup SQL.
- Added essentials bundles, food badges, seller trust passport, organic corner, autoship essentials, no-bad-image replacement rule, and homepage priority sorting.
