TRUSTCART FINAL DEPLOYMENT INSTRUCTIONS

1. Backup database first.
2. For existing database, run this SQL once:
   database/migration-admin-security-hotfix.sql
3. Push/deploy this full project.
4. Clear Render build cache if previous compile errors remain.
5. Test URLs:
   /buyer/login
   /seller/login
   /command-center/login
   /seller/reviews
   /command-center/reviews
6. Admin credentials:
   admin@trustcart.ph / trustadmin2026
7. Buyer credentials:
   buyer@trustcart.ph / trust123
8. Seller credentials:
   lagunafarmers@trustcart.ph / trust123

Important:
- Admin pages are protected by /command-center/login
   /seller/reviews
   /command-center/reviews.
- Chat/ticket/review attachments are saved locally in /uploads and served at /uploads/**.
- If deployed on ephemeral storage, uploads may reset on redeploy. This is acceptable for prototype demo. For production, use cloud storage.
